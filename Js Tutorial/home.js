var username=prompt("What is your name?","your name");

if(username==="Darius") {
    document.write("<h1>Hello " + username + " You won an Nissan GTR</h1>");
} else {
    document.write("<h1>Hello " + username + "!");
}